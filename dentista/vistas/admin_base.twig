<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block titulo %}Admin Panel{% endblock %}</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-image: url('/vistas/img/fondo.jpg');
            background-size: cover;
            background-attachment: fixed;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            background: white;
            width: 100%;
            max-width: 1000px;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            height: fit-content;
        }

        h1 {
            color: #333;
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }

        th {
            background: #f4f4f4;
            color: #333;
            opacity: 1;
        }

        tr:hover {
            background: #f9f9f9;
        }

        .btn {
            padding: 8px 15px;
            border-radius: 4px;
            text-decoration: none;
            color: white;
            display: inline-block;
            font-size: 0.9rem;
            border: none;
            cursor: pointer;
        }

        .btn-green {
            background: #28a745;
        }

        .btn-red {
            background: #dc3545;
        }

        .btn-blue {
            background: #007bff;
        }

        .btn-orange {
            background: #ffc107;
            color: #333;
        }

        .footer-link {
            display: block;
            margin-top: 20px;
            text-align: center;
            color: #666;
            text-decoration: none;
        }

        .footer-link:hover {
            color: #000;
            text-decoration: underline;
        }

        .filtros {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            display: flex;
            gap: 10px;
            align-items: flex-end;
            margin-bottom: 20px;
        }

        {% block estilos_extra %}{% endblock %}
    </style>
</head>
<body>
<div class="container">
    {% block contenido %}{% endblock %}
</div>
</body>
</html>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Mis Citas</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: 'Segoe UI', sans-serif;
            background-image: url('/vistas/img/4820170_2463130.jpg');
            background-size: cover;
            background-position: center center;
            background-attachment: fixed;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .contenedor {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 900px;
            text-align: center;
        }

        h1 { color: #2c3e50; margin-bottom: 30px; }

        .tabla-citas {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        .tabla-citas th, .tabla-citas td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        .tabla-citas th {
            background-color: #f8f9fa;
            color: #333;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 0.85rem;
        }

        .tabla-citas tr:hover { background-color: #f1f1f1; }

        .estado {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: bold;
            color: white;
            text-transform: capitalize;
        }

        .pendiente  { background-color: #f0ad4e; }
        .confirmada { background-color: #28a745; }
        .rechazada  { background-color: #dc3545; }

        .boton-volver {
            display: inline-block;
            padding: 10px 20px;
            background-color: #6c757d;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }
        .boton-volver:hover { background-color: #5a6268; }

        .mensaje-vacio {
            color: #666;
            font-style: italic;
            background: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
        }
    </style>
</head>
<body>

<div class="contenedor">
    <h1>Mis Citas Médicas</h1>

    {% if citas is not empty %}
        <div style="overflow-x:auto;">
            <table class="tabla-citas">
                <thead>
                <tr>
                    <th>Fecha</th>
                    <th>Hora</th>
                    <th>Servicio(s)</th>
                    <th>Estado</th>
                </tr>
                </thead>
                <tbody>
                {% for cita in citas %}
                    <tr>
                        <td>{{ cita.fecha_cita | date("d/m/Y") }}</td>
                        <td>{{ cita.fecha_cita | date("H:i") }}</td>

                        <!-- Esta linea no sería necesaria si yo no quisiese añadir
                         citas de forma manual para hacer pruebas-->
                        <td>
                            {{ cita.servicios ? cita.servicios : 'Sin servicio especificado' }}
                        </td>

                        <td>
                                    <span class="estado {{ cita.nombre_estado|lower }}">
                                        {{ cita.nombre_estado }}
                                    </span>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <div class="mensaje-vacio">
            <p>No tienes ninguna cita registrada.</p>
        </div>
    {% endif %}

    <a href="/" class="boton-volver">⬅ Volver al Panel</a>
</div>

</body>
</html>